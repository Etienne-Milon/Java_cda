package application;


import javafx.fxml.FXML;
import javafx.scene.control.Button;
import javafx.scene.control.Label;
import javafx.event.Event;

public class SampleController {
	
	@FXML
	Label label;
	
	@FXML
	public String onClick(Event event) {
		Button button = (Button) event.getSource();
		String stackInput = "";
		stackInput = stackInput + button.getText();
		label.setText(stackInput);
		return stackInput;
	}
	
	@FXML
	public void getAfficheur(Event event) {
		
		
	}
	
}
